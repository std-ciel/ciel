int main() {
    for (int i = 0; i < 10; i++) { int temp = i; }

    int w = 0;
    while (w < 5) { w = w + 1; }

    int d = 0;
    do { d = d + 1; } while (d < 3);

    int u = 0;
    until (u == 2) { u = u + 1; }

    int arr[5];
    for (int j = 0; j < 5; j++) { arr[j] = j; }

    for (int a = 0; a < 2; a++) {
        for (int b = 0; b < 2; b++) { int t = a + b; }
    }
    int o = 0;
    while (o < 2) {
        int p = 0;
        while (p < 2) { p = p + 1; }
        o = o + 1;
    }

    int k = 0;
    until (k > 2 && k < 5) { k = k + 1; }

    int s = 0;
    for (int i2 = 0; i2 < 10; i2++) {
        if (i2 % 2 == 0) continue;
        s = s + i2;
        if (s > 10) break;
    }
    return 0;
}
