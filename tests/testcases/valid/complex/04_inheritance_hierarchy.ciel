// Advanced inheritance and polymorphism tests

class Animal {
    protected:
        int age;
        char name[50];
    public:
        Animal(int initial_age) { this->age = initial_age; }
        void set_age(int new_age) { this->age = new_age; }
        int get_age() { return this->age; }
};

class Mammal : public Animal {
    protected:
        bool warm_blooded;
    public:
        Mammal(int initial_age) { this->age = initial_age; this->warm_blooded = 1 == 1; }
        void set_warm_blooded(bool wb) { this->warm_blooded = wb; }
};

class Dog : public Mammal {
    private:
        char breed[30];
    public:
        Dog(int initial_age) { this->age = initial_age; }
        void bark() { this->age = this->age + 1; }
        void set_breed(char* breed_name) {}
};

// Multiple inheritance levels
class Vehicle {
    protected:
        int speed;
    public:
        Vehicle(int initial_speed) { this->speed = initial_speed; }
        void accelerate(int amount) { this->speed = this->speed + amount; }
};

class Car : public Vehicle {
    private:
        int doors;
    public:
        Car(int initial_speed, int num_doors) { this->speed = initial_speed; this->doors = num_doors; }
        void honk() { this->speed = this->speed + 1; }
};

class SportsCar : public Car {
    private:
        int turbo_boost;
    public:
        SportsCar(int initial_speed, int num_doors, int turbo) { this->speed = initial_speed; this->turbo_boost = turbo; }
        void activate_turbo() { this->speed = this->speed + this->turbo_boost; }
};

// Different access specifiers in inheritance
class ProtectedBase {
    protected:
        int prot_val;
    public:
        int pub_val;
};

class ProtectedDerived : protected ProtectedBase {
    public:
        void access_protected() { this->prot_val = 0; }
};

class PrivateDerived : private ProtectedBase {
    public:
        void access_private() { this->pub_val = 0; }
};

// Functions returning inherited types
class Animal* create_animal() {
    return (class Animal*)0;
}

class Dog* create_dog() {
    return (class Dog*)0;
}

class Dog** create_dog_pack(int size) {
    return (class Dog**)0;
}

class SportsCar* build_sports_car(int speed, int doors, int turbo) {
    return (class SportsCar*)0;
}

// Complex hierarchy functions
int train_dog(class Dog* d, class Animal* trainer) {
    return 0;
}

class Car* upgrade_vehicle(class Vehicle* v, int new_features, ...) {
    return (class Car*)0;
}

int main() {
    class Animal a;
    class Mammal m;
    class Dog d;
    class Vehicle v;
    class Car c;
    class SportsCar sc;

    class Animal* a_ptr;
    class Dog* d_ptr;
    class SportsCar* sc_ptr;

    return 0;
}
