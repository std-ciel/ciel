// Test typedef declarations

// Basic type aliases
typedef int Integer;
typedef char Character;
typedef float Real;
typedef unsigned Counter;
typedef bool Flag;

// Pointer typedefs
typedef int* IntPtr;
typedef char* String;
typedef float** FloatPtrPtr;

struct Point {
    int x;
    int y;
};

struct Node {
    int data;
    struct Node* next;
};

union Data {
    int i;
    float f;
    char c;
};

enum Color {
    RED,
    GREEN,
    BLUE
};

// Struct typedef
typedef struct Point Point;

typedef struct Node Node;

// Union typedef
typedef union Data Data;

// Enum typedef
typedef enum Color Color;


typedef struct Inline{
    int x;
    int y;
} Try;


// Function pointer typedef (if supported)
// typedef int (*FuncPtr)(int, int);

int main() {
    // Using typedefs
    Integer x = 10;
    String s = (String)0;
    Point p;
    Node n;
    Data d;
    Color c;
    
    return 0;
}
