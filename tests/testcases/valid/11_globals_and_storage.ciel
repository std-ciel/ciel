int global_counter = 0;

void increment() {
    static int calls = 0;
    calls = calls + 1;
    global_counter = global_counter + 1;
}

int main() {
    increment();
    increment();
    int gc = global_counter;
    return 0;
}
