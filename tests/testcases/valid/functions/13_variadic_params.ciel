// Test variadic function parameters

// Variadic functions with various return types
int sum_ints(int count, ...) {
    return 0;
}

float average(int count, ...) {
    return 0.0;
}

char* concat_strings(char* first, ...) {
    return (char *)0;
}

void* generic_func(int type, ...) {
    return (void *)0;
}

// Pointer returns with variadic params
int* create_array(int size, ...) {
    return (int *)0;
}

char** build_string_list(int count, ...) {
    return (char **)0;
}

// Mixed parameter types with variadic
int process(int a, char c, float f, ...) {
    return 0;
}

unsigned* complex_variadic(int* ptr, char** strs, ...) {
    return (unsigned *)0;
}

// No fixed parameters before variadic (just ellipsis - edge case)
// Note: Standard C requires at least one fixed parameter

int main() {
    return 0;
}
