// Test enum return types

enum Color {
    RED,
    GREEN,
    BLUE
};

enum Status {
    SUCCESS,
    FAILURE,
    PENDING
};

enum Direction {
    NORTH,
    SOUTH,
    EAST,
    WEST
};

// Return enum by value
enum Color get_color() {
    return Color.RED;
}

enum Status get_status() {
    return Status.SUCCESS;
}

// Return pointer to enum
enum Color* get_color_ptr() {
    return (enum Color*)0;
}

enum Direction* get_direction_ptr() {
    return (enum Direction*)0;
}

// Return double pointer to enum
enum Status** get_status_array() {
    return (enum Status**)0;
}

// Functions with enum parameters
enum Color process_color(enum Color c) {
    return c;
}

enum Status* check_status(enum Status* s) {
    return s;
}

int main() {
    return 0;
}
